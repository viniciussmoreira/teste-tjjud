@model TesteTJJUD.Models.Autor

@{
    ViewBag.Title = "Details";
}

<h2 class="text-primary">@Model.Nome</h2>

<div class="card">
    <div class="card-header bg-dark text-white">
        
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.CodAu)</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.CodAu)</dd>

            <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Nome)</dt>
            <dd class="col-sm-9">@Html.DisplayFor(model => model.Nome)</dd>

            
        </dl>
    </div>
</div>

<p class="mt-3">
    @Html.ActionLink("Edit", "Edit", new { id = Model.CodAu }, new { @class = "btn btn-warning" })
    @Html.ActionLink("Voltar para Lista", "Index", null, new { @class = "btn btn-secondary" })
</p>
