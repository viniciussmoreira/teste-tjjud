@model TesteTJJUD.Models.Autor
@{
    ViewBag.Title = "Edit";
}

@using (Html.BeginForm())
{
    <div>
        <label>Nome</label>
        @Html.TextBoxFor(model => model.Nome, new { @class = "form-control", @maxlength = "40" })
    </div>
    <button type="submit" class="btn btn-primary">Salvar</button>
}